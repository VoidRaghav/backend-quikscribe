apiVersion: v1
kind: Secret
metadata:
  name: redis-credentials
  namespace: quikscribe
  labels:
    app: quikscribe

type: Opaque
stringData:
  REDIS_URL: "redis://your-elasticache-endpoint:6379"
  # Host:port format for KEDA and addressFromEnv
  REDIS_ADDRESS: "your-elasticache-endpoint:6379"
